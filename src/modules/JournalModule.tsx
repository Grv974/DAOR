import { ModulePlaceholder } from './Placeholder';

export function JournalModule() {
  return (
    <ModulePlaceholder
      name="Journal de progression"
      desc="Entrées datées et typées, @mentions auto-reliées (contacts, projets, objectifs), revues guidées pré-remplies, timeline et recherche."
    />
  );
}
