import { ModulePlaceholder } from './Placeholder';

export function CrmModule() {
  return (
    <ModulePlaceholder
      name="CRM relationnel"
      desc="Fiches contacts, cadence de suivi & relances de contacts froids, scoring relationnel, promesses & demandes, historique des interactions."
    />
  );
}
